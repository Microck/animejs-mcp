# Installation for Codex

1. Clone the repository:
   ```bash
   git clone https://github.com/Microck/animejs-mcp.git
   cd animejs-mcp
   npm install
   ```

2. Add to your MCP config:
   ```json
   "animejs": {
     "command": "node",
     "args": ["/path/to/animejs-mcp/index.js"]
   }
   ```
